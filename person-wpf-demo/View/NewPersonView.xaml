<UserControl x:Class="person_wpf_demo.View.NewPersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:person_wpf_demo.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Label Width="200" Margin="10">Prenom</Label>
            <TextBox Text="{Binding Prenom, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="10"/>
            <Label Width="200" Margin="10">Nom</Label>
            <TextBox Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="10"/>
            <TextBlock Text="{Binding ErrorMessages, UpdateSourceTrigger=PropertyChanged}" Foreground="Red" HorizontalAlignment="Center" Margin="10"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Width="200" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
